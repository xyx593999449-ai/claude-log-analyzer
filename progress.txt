# Ralph Progress Log
Started: Wed Apr 15 17:10:30 CST 2026

## Codebase Patterns
- Dashboard 与新页面的数据契约统一收敛在 `src/lib/dashboardTypes.ts`，新增页面模型优先在这里定义并复用。
- 该项目的质量门禁以 `npm run lint`（`tsc --noEmit`）为主，功能迭代后建议补跑 `npm run build` 做构建级回归。

## [2026-04-15 17:14:06 CST] - US-001
- 完成 HITL 迭代运营页首批类型模型，覆盖迭代批次总览、核实/质检问题分析、迭代建议、候选版本、联合回归结果与发布结论。
- 在类型层显式区分了人工反馈结果池、迭代批次和任务执行批次，避免后续页面开发时概念混淆。
- 补充 `HITLIterationPageState` 作为页面状态/视图模型，为后续路由和模块渲染提供统一状态结构。
- Files changed: `src/lib/dashboardTypes.ts`, `README.md`, `CHANGELOG.md`, `progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: Dashboard 相关契约集中在 `src/lib/dashboardTypes.ts`，后续页面与 API 类型应延续该收敛方式。
  - Gotchas encountered: 仓库默认主分支是 `master`，不是 `main`；按 PRD 切分支时需要兼容该事实。
  - Useful context: 当前项目未配置独立 test 脚本，类型检查与构建检查是本仓的核心质量门禁。
---
