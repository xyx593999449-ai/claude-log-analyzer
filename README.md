# Claude Log Analyzer

面向大 POI 核实与质检场景的日志分析与流程看板。

项目支持日志导入、指标总览、任务流程列表和单任务日志详情分析，适合用于核实链路与质检链路的日常巡检、异常定位和结果复核。

## 1. 核心能力

- 日志导入：支持核实日志与质检日志上传，并触发结果刷新。
- 首页看板：集中展示任务总量、自动化率、核实质量、异常任务等核心指标。
- 流程列表：按任务展示流程进度、核实模块、质检模块、异常风险与元信息。
- 日志详情：支持核实 / 质检阶段切换，查看时间线、工具调用、Token 成本与原始日志。

## 2. 当前界面结构

### 2.1 首页

- 顶部为日志导入区，默认收起。
- 核实与质检执行概览同一行横向对比。
- 首页保留任务总量、执行概览、流程分布和异常标签等核心信息。
- 当前页流程分布使用整行平铺的任务流转状态板展示，每个状态直接显示任务量和占比。
- 当前页异常信息放在流程分布下方，使用 tag 聚合展示。
- 任务详情使用流程列表卡片展示。

### 2.2 任务详情卡片

- 默认展示任务摘要、流程轨道、证据摘要和异常标签。
- 证据摘要读取 `poi_verified.evidence_record`，展示证据总量与各 `source_name` 的条数。
- 点击“展开更多信息”后展示核实模块、质检模块、任务元信息、异常与风险和日志详情入口。
- 核实模块与质检模块支持共享展开按钮，便于同时查看摘要与执行明细。
- 当前流程节点使用靛蓝色高亮。
- 执行异常、需人工介入、质检不通过等状态会重点高亮。

### 2.3 日志详情页

- 使用与首页一致的卡片化风格。
- 顶部展示任务摘要和阶段切换。
- 中部展示单阶段日志分析结果。
- 滚动区域使用接近 IDE / VS Code 风格的滚动条。
- 执行异常会单独以告警卡形式展示。

## 3. 目录说明

| 模块 | 说明 | 目录 |
|---|---|---|
| 前端看板 | 首页、任务详情列表、日志详情页 | `src/components/dashboard` |
| 日志分析组件 | 时间线、工具分析、Token 分析、原始日志 | `src/components/legacy` |
| 前端数据层 | 接口请求、类型定义、解析工具 | `src/lib` |
| 后端接口 | 看板接口、任务接口、导入接口 | `server` |

## 4. 启动方式

### 4.1 安装依赖

```bash
npm install
```

### 4.2 本地开发

```bash
# 前端
npm run dev:web

# 后端
npm run dev:api
```

默认地址：

- 前端：`http://localhost:3000`
- 后端：`http://localhost:3001`

### 4.3 检查与构建

```bash
npm run lint
npm run build
```

## 5. 指标口径

- 自动化率：`1 - (需人工核实任务数 / 已有核实结论任务数)`
- 核实质量：`(核实通过且质检通过 + 需人工核实且质检不通过) / 已质检总量`
- 成本口径：
  - 输入 Token：`4 元 / 百万 Token`
  - 输出 Token：`18 元 / 百万 Token`
  - 平均成本：`总成本 / 任务数`

## 6. 关键文件

```text
src/
|-- components/
|   |-- dashboard/
|   `-- legacy/
|-- lib/
|-- App.tsx
`-- main.tsx
server/
README.md
CHANGELOG.md
```

## 7. 当前状态

- `npm run lint` 已通过
- `npm run build` 已通过
- 当前构建产物已完成收敛，暂无构建告警
