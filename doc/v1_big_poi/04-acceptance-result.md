# 大 POI 看板迭代验收记录（本地）

- 验收日期: 2026-03-18
- 验收范围: 本次迭代新增首页看板、日志导入、任务详情、日志详情、缓存清理
- 说明: 当前环境网络受限，`npm install` 未完整成功，前后端进程未能实际拉起；以下结论基于代码走查与静态校验。

## 验收结论
- 结果: 部分通过（功能代码已落地，运行态验收待补）
- 阻塞项: 依赖安装受限，无法执行 `tsc / vite / tsx` 与端到端手工联调

## 用例执行结果

1. 新首页作为默认页
- 预期: 打开应用默认进入核实/质检看板
- 实际: `src/App.tsx` 默认渲染 `DashboardHome`
- 结果: 通过（代码级）

2. 概要四模块展示
- 预期: 所有任务、已核实任务概览、质检概览、人工任务监控
- 实际: `DashboardHome` 已实现四张卡片；后端 `getOverview` 提供对应字段
- 结果: 通过（代码级）

3. 日志上传双窗口
- 预期: 分核实、质检两个上传区，支持导入并落表
- 实际: 前端双上传区（核实/质检），API `POST /api/dashboard/import`，服务层聚合后写 `temp_task_analysis`
- 结果: 通过（代码级）

4. 缓存清理只清落表
- 预期: 仅清理日志分析落表，不改业务表
- 实际: `clearAnalysisCache` 只删除 `temp_task_analysis` 与 `analysis_imports`
- 结果: 通过（代码级）

5. 详情按任务维度展示
- 预期: 每任务一行，展示 poi 基础信息、执行状态、核实与质检结果、可展开详情
- 实际: 任务表行已包含上述字段，执行详情通过 `details` 展开
- 结果: 通过（代码级）

6. 不一致状态标识
- 预期: 以数据库结果为准，日志冲突时标记告警
- 实际: 仓储层生成 `verify_mismatch_reason` / `qc_mismatch_reason`，前端显示警示图标和悬浮说明
- 结果: 通过（代码级）

7. 人工任务规则
- 预期: `poi_qc.is_manual_required = 1` 或 `poi_verified.verify_status = 需人工核实`
- 实际: 概要与列表过滤均按该规则处理
- 结果: 通过（代码级）

8. 详情分页与页大小设置
- 预期: 默认分页，支持每页条数切换
- 实际: 默认 20，支持 20/50/100
- 结果: 通过（代码级）

9. 日志详情页面
- 预期: 首页可跳转日志详情页，展示核实/质检日志
- 实际: `TaskLogPage` 已实现双面板详情，按 task_id 查询
- 结果: 通过（代码级）

10. PostgreSQL 可替换性
- 预期: 支持后续切正式 PG
- 实际: 当前为 SQLite mock 实现，查询逻辑集中在 `server/repository.ts`，已具备替换入口但未实现 PG 驱动
- 结果: 部分通过（架构预留，未接 PG）

## 待补验收（解除环境限制后）
- 执行 `npm install` 完整安装依赖
- 执行 `npm run dev:api` + `npm run dev:web` 联调
- 上传 `example` 目录日志做真实导入
- 对照页面核对四模块数值与抽样任务详情
- 补跑异常筛选、人工任务筛选、分页翻页、清缓存回归
